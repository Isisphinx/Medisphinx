import { createSlice } from '@reduxjs/toolkit'

const initialState = {}

const myStepperListSlice = createSlice({
  name: 'fakeliste',
  initialState,
  reducers: {
  },
})

export const { } = myStepperListSlice.actions
export default myStepperListSlice.reducer